/**
 * 将剪贴板里的文件拷贝到当前目录
 * @param targetPathname 目标目录
 * @param keepOrigin true保留文件，false删除源文件
 */
const copyFilesFromClipboard = (targetPathname, keepOrigin=true) => (dispatch, getState) => {

};

export default module.exports = copyFilesFromClipboard