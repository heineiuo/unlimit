
/**
 * hub's default config
 * {
 *   port: number, // socket.io listing port
 *   presets: Array // preset services
 * }
 */

const defaultConfig = {
  port: 3311,
  presets: []
};

export default module.exports = defaultConfig