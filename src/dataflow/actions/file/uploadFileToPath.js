import * as API from '../../../sdk/api'

const uploadFileToPath = () => {
  return async (dispatch) => {
    try {

      // url: api['uploadImage'][2]+'?'+encodeQuery(formData),

    } catch(e){
      console.error(e)
      alert()
    }
  }
}

export default uploadFileToPath