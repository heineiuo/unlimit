import * as API from '../../../sdk/api'

/**
 * 获取路由配置详情
 */

const getRouterDetail = (location_id) => {
  return async (dispatch, getState) => {
    try {


    } catch(e){
      console.error(e)
      alert(e)
    }
  }
}

export default getRouterDetail